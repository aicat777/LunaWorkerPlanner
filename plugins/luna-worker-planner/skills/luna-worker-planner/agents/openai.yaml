interface:
  display_name: "Luna Worker Planner"
  short_description: "Plan, delegate, and critically evaluate results."
  default_prompt: "Use $luna-worker-planner as the project planner and controller: delegate project work to luna_worker, critically reason over its final report without independently inspecting the project, delegate any needed fact-checking, and then make your own planning decision."

policy:
  allow_implicit_invocation: true
