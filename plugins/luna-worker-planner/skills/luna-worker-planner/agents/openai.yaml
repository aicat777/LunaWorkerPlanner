interface:
  display_name: "Luna Worker Planner"
  short_description: "Plan, delegate, and critically evaluate results."
  default_prompt: "Use $luna-worker-planner as the project planner and controller: delegate needed project work to luna_worker, wait for its final report, critically assess its evidence, reasoning, coverage, and risks, and then make your own planning decision without executing project work in the parent."

policy:
  allow_implicit_invocation: true
